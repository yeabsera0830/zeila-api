import filters from '../../core/values/filters';
import strings from '../../common/values/strings';

export default filterTextMap = {
    [filters.CATEGORY]: strings.category,
    [filters.LOCATION]: strings.location,
    [filters.PRICE]: strings.price,
    [filters.SERVICES]: strings.services,
    [filters.OPEN_DAYS]: strings.openDays,
    [filters.OPEN_HOURS]: strings.openHours,
    [filters.VIBE]: strings.vibe
};